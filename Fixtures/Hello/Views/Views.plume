@component Page(title: String, items: [Item]) {<!doctype html>
<html><head><title>{title}</title></head>
<body><h1>{title}</h1>@if items.size > 0 {<ul>@for item in items {<li>{forloop.index}. {item.name}</li>}</ul>} else {<p>No items.</p>}</body></html>}
